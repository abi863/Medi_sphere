apiVersion: v1
kind: Service
metadata:
  name: kafka-service
  namespace: default
  labels:
    app: medisphere-kafka
spec:
  type: ClusterIP
  ports:
    - port: 9092
      targetPort: 9092
      protocol: TCP
      name: kafka
  selector:
    app: medisphere-kafka
